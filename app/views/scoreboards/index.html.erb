<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Infamist</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <style></style>
  </head>
  <body id="b-t">
<div class="container-fluid mx-auto">
    <!-- top left "Top List" should have the top 10 most popular list items with their associated .gifs -->
    <div id="big-div" class="container text-center">
      <div class="row">
        <div id="home-banner" class="col-lg-8 well">
          <div id="home-banner-title" class="well">
            <h1>
              <b>The worst things</b>
            </h1>
          </div>
          <%for i in 0..9%>
            <p id="header-text">
              <b><%=i+1%>.
                <%= @scoreboard[i][0].name.capitalize%></b>
            </p>
            <p>Score:
              <%= @scoreboard[i][1].round(1)%></p>
            <div id="home-banner" class="thumbnail">
              <p><img id="myImage" src=<%=@scoreboard[i][0].img_url%> alt="Gif of <%=@scoreboard[i][0].name%>"><br><br></p>
            </div>
          <%end%>
        </div>

        <!-- This should be a feed of the most recent list items -->
        <div class="container text-center">
          <div class="row">
            <div class="col-sm-4">
              <div id="small-div" class="well">
                <div id="home-banner-title" class="well">
                <h1>
                  <b>The latest trash</b>
                </h1>
              </div>

                  <% @items.each do |item| %>

                    <% if item.id > (@items.last.id - 11) %>
                      <div class="thumbnail">
                        <%=item.name%>
                        <img id="prof_images" src=<%=item.img_url%>
                      </div>
                      </div>
                    <%end%>
                  <%end%>
                </div>
                </div>
                </div>



              </div>




  </body>
</html>
