<%= form_with(model: glist, local: true) do |form| %>
  <% if glist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(glist.errors.count, "error") %> prohibited this glist from being saved:</h2>

      <ul>
      <% glist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
