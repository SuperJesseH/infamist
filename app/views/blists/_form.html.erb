<%= form_with(model: blist, local: true) do |form| %>
  <% if blist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blist.errors.count, "error") %> prohibited this blist from being saved:</h2>

      <ul>
      <% blist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
