<%= form_with(model: glist_item, local: true) do |form| %>
  <% if glist_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(glist_item.errors.count, "error") %> prohibited this glist_item from being saved:</h2>

      <ul>
      <% glist_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
